fun fib(a) {
    if (a <= 2) {
        return 1;
    }
    return fib(a - 1) + fib(a - 2);
}

var time = clock();
print fib(27);
var after = clock();

prints((after - time) / 1000, " seconds to compute");
